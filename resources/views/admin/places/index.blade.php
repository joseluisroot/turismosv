<!DOCTYPE html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>Catálogo — TurismoSV</title><link rel="icon" type="image/svg+xml" href="{{ asset('favicon.svg') }}">@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="catalog-admin-page">
<header class="place-header"><a href="{{ route('home') }}"><img src="{{ asset('brand/logo-turismosv.svg') }}" alt="TurismoSV"></a><nav><a href="{{ route('admin.moderation.index') }}">Moderación</a><a href="{{ route('profile') }}">Mi perfil</a></nav></header>
<main class="catalog-admin-shell"><div class="catalog-admin-heading"><div><p class="eyebrow">Área editorial privada</p><h1>Catálogo de lugares</h1><p>Publica solamente información respaldada por una fuente identificable y una fecha de verificación.</p></div><a class="primary-button" href="{{ route('admin.places.create') }}">Crear lugar</a></div>@if(session('catalog_status'))<div class="status-message">{{ session('catalog_status') }}</div>@endif
<section class="catalog-table-section"><div class="section-heading"><div><p class="eyebrow">{{ $places->total() }} registros</p><h2>Fichas editoriales</h2></div></div><div class="catalog-table">@foreach($places as $place)<article><div><span class="publication-{{ $place->publication_status }}">{{ match($place->publication_status){'published'=>'Publicado','archived'=>'Archivado',default=>'Borrador'} }}</span><small>{{ $place->category->name }} · {{ $place->department->name }}</small><strong>{{ $place->name }}</strong></div><div><small>Fuente</small><span>{{ $place->source_name ?: 'Pendiente' }}</span></div><div><small>Verificada</small><span>{{ $place->source_verified_at?->translatedFormat('d M Y') ?: '—' }}</span></div><a href="{{ route('admin.places.edit',$place) }}">Editar →</a></article>@endforeach</div>{{ $places->links() }}</section>
<section class="taxonomy-admin"><div><h2>Categorías</h2><form method="post" action="{{ route('admin.categories.store') }}" class="taxonomy-create">@csrf<input name="name" placeholder="Nueva categoría" required><input name="icon" maxlength="16" placeholder="Icono"><input name="description" maxlength="500" placeholder="Descripción" required><button class="approve-button">Crear</button></form>@foreach($categories as $category)<form method="post" action="{{ route('admin.categories.update',$category) }}" class="taxonomy-row">@csrf @method('PUT')<input name="name" value="{{ $category->name }}" required><input name="icon" value="{{ $category->icon }}"><input name="description" value="{{ $category->description }}" required><small>{{ $category->places_count }} lugares</small><button>Guardar</button></form>@endforeach</div><div><h2>Departamentos</h2><form method="post" action="{{ route('admin.departments.store') }}" class="taxonomy-create">@csrf<input name="name" placeholder="Nuevo departamento" required><button class="approve-button">Crear</button></form>@foreach($departments as $department)<form method="post" action="{{ route('admin.departments.update',$department) }}" class="taxonomy-row compact">@csrf @method('PUT')<input name="name" value="{{ $department->name }}" required><small>{{ $department->places_count }} lugares</small><button>Guardar</button></form>@endforeach</div></section></main></body></html>
