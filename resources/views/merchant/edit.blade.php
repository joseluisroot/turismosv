@extends('layouts.account')
@section('title','Administrar '.$place->name)
@section('content')
<p class="eyebrow">Ficha comercial verificada</p><h2>{{ $place->name }}</h2><p class="form-intro">Actualiza datos comprobables. No tienes acceso a estrellas, reseñas, ranking ni métricas comunitarias.</p>@if(session('merchant_status'))<div class="status-message">{{ session('merchant_status') }}</div>@endif
<form method="post" action="{{ route('merchant.places.update',$place) }}" class="merchant-form">@csrf @method('PUT')<label>Descripción oficial<textarea name="official_description" maxlength="1500" rows="5">{{ old('official_description',$place->official_description) }}</textarea></label><label>Teléfono<input name="official_phone" maxlength="30" value="{{ old('official_phone',$place->official_phone) }}"></label><label>WhatsApp<input name="official_whatsapp" maxlength="30" value="{{ old('official_whatsapp',$place->official_whatsapp) }}"></label><label>Sitio web<input type="url" name="official_website" maxlength="191" value="{{ old('official_website',$place->official_website) }}" placeholder="https://"></label><label>Dirección pública<input name="official_address" maxlength="191" value="{{ old('official_address',$place->official_address) }}"></label><label>Horarios<textarea name="official_opening_hours" maxlength="1000" rows="4">{{ old('official_opening_hours',$place->official_opening_hours) }}</textarea></label><label>Referencia de precios<input name="official_price_reference" maxlength="120" value="{{ old('official_price_reference',$place->official_price_reference) }}"></label>@if($errors->any())<div class="status-error">Revisa los datos señalados e intenta nuevamente.</div>@endif<button class="primary-button" type="submit">Guardar información oficial</button></form><a class="text-button profile-explore" href="{{ route('places.show',$place) }}">Ver ficha pública →</a>
@endsection
